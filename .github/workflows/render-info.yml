name: Render account info

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    env:
      RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
    steps:
      - uses: actions/checkout@v4
      - name: Inventory
        run: bash scripts/render-info.sh
